# MiniWorld 仪表盘改造方案

## 1. 目标与边界

本次改造把现有仪表盘重组为“活体世界观测站”：先判断运行是否安全，再观察世界与五位 NPC，随后阅读关键变化，最后按需进入趋势、解释和审计。

必须保留的边界：

- 保留全部现有功能、控制能力、公开 API 和字段语义。
- 不改变 Simulation Engine 对客观事实和动作执行的唯一权威。
- 不把低频模块删除或隐藏到不可发现；只改变默认展开层级和进入方式。
- 新聚合接口只增量添加，必须支持局部错误、统一快照标识和旧接口逐模块回退。
- 保持 FastAPI + 原生 HTML/CSS/JavaScript，不引入前端框架或构建链。

## 2. 当前问题与源码证据

### 信息结构

- 首页依次平铺地点、运行时、Agent、会话、认知、产品、经济、职业、社区、社交、故事和事件，几乎所有模块视觉权重相同。
- 缺少页内导航、异常汇总和待处理事项入口，用户需要长距离滚动才能重新定位。
- 运行安全、世界控制和 Agent 控制分散，普通状态与危险操作的视觉距离不足。

### NPC 详情

- 当前右侧抽屉一次性串联 17 个以上区块，包括经济、职业、生活节奏、社交、人生、目标、状态、性格、关系、对话、认知、记忆、Agent 和决策。
- 打开任意 NPC 会同时发起 18 个请求；多数数据在用户尚未滚动到对应区域时已经加载。
- 抽屉没有分区导航、深链接或加载边界，窄屏仅变成全屏长页面。

### 请求与渲染

- 首页刷新一次发起约 35 个 GET 请求，每 2 秒执行一次；按源码轮询节奏，页面可见时最多约 1,050 次请求/分钟。
- NPC 详情打开后额外每秒请求 3 个端点，稳定态最多增加约 180 次请求/分钟，并有首次 18 请求突发。
- 没有同键并发锁、AbortController 或页面不可见暂停；慢请求可能与下一轮刷新重叠。
- 顶层 `Promise.all` 中任一关键请求失败会中断整轮首页渲染；页面缺少模块级陈旧状态和局部重试。
- 大量区域用 `innerHTML` 整体替换，容易造成不必要的 DOM 工作，并可能打断焦点、选区和辅助技术上下文。

### 响应式与可访问性

- 现有响应式主要将网格逐步降为单列，没有针对导航、危险操作、详情标签、时间线和长数据行设计专门行为。
- 多处 9–11px 元数据不适合长期阅读；部分按钮未保证 44px 触控高度。
- NPC 对话框缺少完整的焦点捕获、打开后初始焦点和关闭后焦点恢复策略。

## 3. 首页信息架构

首页按以下固定顺序组织。

### A. 全局顶部栏

- 左侧：MiniWorld 标识、版本和连接状态。
- 中部：世界日期、时间、暂停状态与速度。
- 右侧：世界暂停/继续和速度控制。
- “重置世界”移入独立的更多/危险操作区，不与日常速度按钮并列。
- 顶部下方提供粘性页内导航：`总览`、`NPC`、`世界脉搏`、`趋势`、`审计`。

### B. 安全与运行状态

这是首屏最高优先级区域，回答四个问题：

1. 世界是否正在安全推进？
2. 在线 Provider 是否真的在被调用？
3. 队列、预算、限流和熔断是否正常？
4. 是否存在需要处理的 fallback、失败或危险状态？

布局包含：

- 单一主状态卡：安全离线、在线、暂停或紧急停止。
- 紧凑指标：Provider 配置、并发/队列、今日调用/Token、估算费用。
- 异常条：只展示当前需要注意的最多 3 项，并提供进入审计的入口。
- 在线自治普通操作使用森林绿；信息/选中使用信号蓝；预算或队列警告使用琥珀；急停单独使用红色并要求确认。

### C. 世界总览

- 保留四个地点及其居民，但把它们组织成世界空间概览，而不是四个同权的大空卡。
- 同时展示世界时间、速度、暂停状态、当前人数分布和活跃行为概况。
- 空地点使用紧凑空状态，避免占用与有人地点相同的垂直空间。
- 点击人物进入其观察档案；点击地点只筛选/聚焦，不引入新的业务行为。

### D. 五位 NPC 状态

固定展示 Alice、Bob、Charlie、Diana、Eric 五张观察卡，每张卡保留并强化：

- 姓名、职业、当前位置和当前行为。
- 能量、饥饿、心情、社交需求四个核心状态。
- Agent 是否启用、正在思考、最终来源或近期 fallback。
- 最重要的一个异常或待处理事项；无异常时显示稳定状态。

桌面端五张卡可在一行或三加二布局中完整扫读；卡片顺序稳定，不因地点变化重新排序。世界总览负责“在哪里”，NPC 区负责“每个人怎么样”。

### E. 关键变化与世界脉搏

- 把最近事件、叙事解释、活跃会话、待履行承诺、fallback 和人生里程碑整理为统一时间线。
- 默认仅展示高价值事件；提供人物、事件类型和“只看异常”筛选。
- 每项固定为时间 → 类型 → 人物 → 事实 → 解释的阅读顺序。
- 事件仍保留原始类型和既有解释语义；不让模型叙事覆盖 Engine 事实。

### F. 趋势与深度模块

经济、职业、社区、社交、故事和产品统计进入“趋势”区域，默认折叠为摘要卡：

- 社会经济：库存、收入/支出、压力和异常。
- 职业与预算：就业、绩效、培训和预算趋势。
- 社区节奏：营业、设施、排班和住房。
- 社交网络：关系阶段、承诺、朋友圈和合住。
- 人生故事：里程碑、周/月总结和回放入口。
- 产品与平衡：存档、统计、引导和平衡审计。

展开后继续展示当前全部数据；折叠状态只影响呈现和请求频率，不改变数据含义。

### G. 深度分析与审计

- Agent 接管总览、会话审计、认知任务、运行时审计和产品检查集中在“审计”区域。
- 首页只显示摘要和异常；完整审计通过折叠区或 NPC 观察档案进入。
- Engine 事实、Utility 建议、Agent 建议、最终行动和 fallback 使用固定的五段式标签，不依靠背景色猜测来源。

## 4. NPC 观察档案

NPC 详情从超长右侧抽屉改为有深链接的观察档案。桌面端可使用宽幅覆盖层或独立详情状态，移动端为全屏页面；两者共享相同信息结构。

### 固定档案头

- 头像、姓名、职业、年龄。
- 当前位置、当前行为、金钱和四项核心状态。
- Agent/Utility 当前来源、在线思考和异常标记。
- 关闭/返回、上一位/下一位 NPC；关闭后焦点返回原 NPC 卡。

### 标签 1：概览

- 当前状态、长期目标和性格摘要。
- 今日排班/生活节奏、住房、归属感与信任摘要。
- 当前最重要的异常、承诺或计划。
- 首次打开只请求这一标签需要的数据。

### 标签 2：决策与自治

- Utility 候选与贡献值。
- Agent 影子建议、Agent 接管建议、Engine 验证与最终真实行动。
- Agent 开关、队列、情绪、计划、fallback 原因和近期审计。
- 固定来源顺序：`Engine 状态 → Utility 建议 → Agent 建议 → Engine 复核 → 最终行动`。

### 标签 3：生活经济

- 职业、工资、表现、班次和技能。
- 预算、经济压力、交易、库存和经济报告。
- 住房、排班、出勤、培训与社区设施可用性。

### 标签 4：关系社交

- 有向关系、双向关系阶段、信任与差异。
- 归属感、承诺、共同活动、朋友圈和合住。
- V1.4 多轮会话与 V1.3/V0.4 兼容对话，明确文字与世界事实的边界。

### 标签 5：记忆与人生

- 记忆时间线和记忆总结。
- 人生里程碑与因果摘要。
- 每日反思、主观信念和 Engine 监控计划；主观内容明确标记为非世界事实。

### 档案行为

- URL 使用 `?npc=<id>&tab=<key>` 保存人物与标签状态，支持刷新、前进/后退和分享本地路径。
- 切换标签时取消旧标签未完成请求；仅加载当前标签，成功后缓存并按各自频率刷新。
- 标签显示加载、局部错误、陈旧数据和重试，不清空其他已成功标签。
- 当档案打开时，首页低频轮询暂停；世界时钟与当前档案高频状态继续刷新。

## 5. 移动端策略

### 断点与布局

- `≥1200px`：宽桌面，多列状态与五人扫读。
- `768–1199px`：平板，两列区域、三加二 NPC 卡、可滚动标签导航。
- `481–767px`：单列首页、紧凑顶部栏、全屏观察档案。
- `≤480px`：控制分两行，指标改为两列或单列，长标签和时间线元数据自动换行。

### 导航与控制

- 顶部栏在窄屏保留世界时间、暂停状态和一个主操作；速度与危险操作进入可展开控制面板。
- 页内导航保持粘性并允许横向滚动，激活项同时使用蓝色文字、浅蓝底和下划/标记。
- 观察档案使用全屏布局，固定档案头与标签栏；内容单列滚动，不出现嵌套横向页面滚动。

### 内容适配

- NPC 卡片从三列信息压缩为“身份/行为 + 状态摘要 + 异常标签”，核心状态采用两列微型指标。
- 时间线改为单轴纵向结构，时间和类型置于内容上方。
- 表格型预算、交易和审计数据在窄屏使用键值行或局部可滚动容器，不压缩到不可读字号。
- 所有触控操作至少 44×44px；正文建议不低于 14px，元数据不低于 12px。
- 支持 `prefers-reduced-motion`，关闭非必要的位移进入动画。

## 6. 请求性能方案

### 第一阶段：前端请求调度，继续使用旧 API

建立单一请求协调器，先解决客户端放大问题：

- **同键并发锁：** 同一个 URL/资源只允许一个在途请求；下一轮复用在途 Promise 或跳过。
- **AbortController：** 标签切换、NPC 切换、档案关闭和页面卸载时取消失去消费者的请求。
- **页面不可见暂停：** `document.hidden` 时停止轮询；恢复可见后立即执行一次按优先级刷新的同步。
- **分频刷新：**
  - 2 秒：世界时钟、运行安全、五位 NPC 当前状态。
  - 5 秒：世界脉搏、会话摘要、fallback 和待处理事项。
  - 30 秒：经济、职业、社区、社交、故事、产品与平衡摘要。
  - 按需：NPC 非当前标签、完整审计和历史列表。
- **局部容错：** 使用模块级 `Promise.allSettled`；保留上次成功数据，显示“更新时间/数据已陈旧”，并允许局部重试。
- **定向失效：** 暂停、速度、Agent 开关等写操作成功后只刷新受影响模块，不再调用整页 `refresh()`。
- **差异化渲染：** 比较模块版本或轻量摘要，只更新发生变化的节点；保留焦点、滚动位置和展开状态。

这一阶段本身不改变任何服务器 API，并作为新聚合接口的回退实现。

### 第二阶段：增量聚合快照接口

新增只读接口：

- `GET /api/dashboard/snapshot?groups=runtime,world,npcs,pulse`
- `GET /api/dashboard/npcs/{npc_id}/snapshot?sections=overview,decision`

建议统一信封：

```json
{
  "schema_version": "1.0",
  "snapshot_id": "<monotonic-or-content-version>",
  "captured_at": "<ISO-8601 UTC timestamp>",
  "world_minute": 12345,
  "modules": {
    "world": { "status": "ok", "version": "...", "data": {} },
    "runtime": { "status": "error", "version": "...", "error": { "code": "...", "message": "...", "retryable": true } }
  }
}
```

约束：

- 所有 world-scoped 模块在同一只读边界内读取，并共享 `world_minute` 与 `snapshot_id`。
- 运行时模块同时返回自身 generation/observed_at，以便解释运行状态与世界事实的采样边界。
- 每个模块独立包装成功或错误；单个查询失败不得把整个响应变成 500。
- 聚合器不通过内部 HTTP 回调旧端点，而是复用同一服务层快照函数，避免额外序列化和锁竞争。
- 响应不包含 Key、prompt、response、隐藏思维链或任何旧 API 不允许暴露的数据。
- 全部旧端点原样保留；聚合接口不成为控制操作入口。

### 第三阶段：渐进迁移与回退

1. 先在不接入 UI 的情况下完成聚合接口契约、局部错误和一致性测试。
2. 前端优先迁移首页高频 `runtime/world/npcs/pulse`。
3. 再迁移 NPC 当前标签；未打开标签仍不请求。
4. 若聚合信封非法、接口不可用或某模块报错，只对该模块调用旧 API 回退。
5. 经过稳定性和请求量验收后保留旧回退路径，不删除旧接口。

### 性能预算

- 可见首页稳定轮询：聚合模式不超过 40 个 GET/分钟；旧 API 回退模式不超过 180 个 GET/分钟。
- 打开 NPC 档案后的稳定轮询不超过 45 个 GET/分钟，不再出现 18 请求的首次突发。
- 同一资源在任何时刻最多一个在途请求；页面隐藏后 1 个调度周期内停止轮询。
- 普通本地环境中，首屏安全状态、世界和五位 NPC 在 1 秒内可用；低频趋势不阻塞首屏。
- 单次高频差异渲染目标低于 50ms，且不得重置键盘焦点、展开状态或档案滚动位置。

## 7. 预计修改的文件

以下为确认后实施阶段的文件范围；本方案阶段不修改这些业务文件。

### 现有前端文件

- `static/index.html`：建立语义化页面骨架、粘性页内导航、首页优先级顺序、折叠区和 NPC 观察档案容器。
- `static/style.css`：落实 DESIGN.md 令牌、状态色纪律、信息层级、响应式布局、焦点、触控尺寸和 reduced-motion。
- `static/app.js`：保留启动与事件入口，接入模块化状态、请求协调、定向失效和路由状态。

### 新增前端模块

- `static/js/api-client.js`：API 调用、错误规范化、AbortController 和旧接口回退。
- `static/js/request-scheduler.js`：同键并发锁、分频、可见性暂停、缓存和模块版本。
- `static/js/dashboard.js`：首页数据映射、局部渲染和折叠状态。
- `static/js/npc-record.js`：NPC 标签、深链接、惰性加载、焦点与关闭恢复。
- `static/js/formatters.js`：迁移现有中文标签、转义和格式化函数，保持数据语义一致。

### 新增与调整的后端文件

- `api/dashboard.py`：新增只读聚合快照路由与查询参数验证。
- `simulation/dashboard.py`：聚合信封、模块级错误包装和只读快照组装辅助函数。
- `simulation/world.py`：增加最小的 dashboard snapshot 服务入口与一致读取边界；不修改既有写路径。
- `app.py`：只增加 dashboard router 注册。

### 测试与文档

- `tests/test_dashboard_api.py`：快照结构、统一版本、局部错误、隐私边界和旧 API 兼容回归。
- `tests/test_dashboard_frontend.py`：静态结构、模块路径和关键可访问性约束；若不引入浏览器测试依赖，则由现有 HTTP 验证脚本补充。
- `scripts/validate_http.py`：加入聚合接口和旧接口并行验证。
- `scripts/benchmark.py`：加入快照延迟、响应体和请求减少量基准。
- `README.md`：记录新快照接口、回退策略、首页结构与验证命令。

实施前先核对仓库是否允许新增原生 ES modules；若必须保持单文件脚本，则上述前端模块先以同名内部模块边界落在 `static/app.js`，不因此改变方案的行为边界。

## 8. 验收标准

### 功能与数据完整性

- 建立一份“现有首页模块与 NPC 区块 → 新位置/标签”的逐项映射，所有现有信息都有可达位置。
- 暂停、继续、速度、重置、运行时启动/暂停/继续/急停、全局/逐 NPC Agent 和认知开关行为保持不变。
- 全部旧 API 契约测试通过；没有字段删除、改名或语义变化。
- Engine 事实、模型文本、估算费用和主观认知继续清晰区分。

### 首页与导航

- 首屏顺序严格为安全与运行状态 → 世界总览 → 五位 NPC → 世界脉搏。
- 用户从任意首页位置最多一次导航动作进入趋势或审计区域。
- 异常状态无需展开低频模块即可被发现；无异常时界面保持安静。

### NPC 观察档案

- 现有 17 个以上区块全部映射到五个标签，无信息丢失。
- 首次打开只加载概览；切换标签才加载相应数据。
- URL 可恢复 NPC 与标签；Escape、关闭按钮和浏览器后退均正确恢复原焦点。
- 单个标签失败不影响其他标签，已成功内容不会被清空。

### 响应式与可访问性

- 在 360×800、768×1024、1440×900 三个基准视口完成截图与交互检查，无页面级横向滚动、遮挡或不可达操作。
- 键盘可完成页内导航、NPC 打开/切换标签/关闭和所有非危险控制。
- 可见焦点、44×44px 触控目标、非颜色唯一状态、对话框语义和 reduced-motion 全部通过人工检查。
- 动态刷新不移动焦点、不折叠用户已展开区域、不把档案滚动位置重置到顶部。

### 请求与稳定性

- 满足本方案性能预算，并记录首页与 NPC 档案的实际每分钟请求量。
- 验证页面隐藏/恢复、慢接口、超时、局部 500、聚合接口不可用和过期响应场景。
- 聚合快照的所有 world-scoped 成功模块共享同一个 `snapshot_id` 与 `world_minute`。
- 注入一个子模块失败时，响应仍为可用信封，其他模块正常渲染并显示各自更新时间。
- 连续运行 30 分钟无重叠请求累积、计时器重复注册或持续内存增长。

## 9. 实施顺序与确认点

1. 建立前端请求协调器和基准记录，界面保持原样。
2. 实施首页骨架、导航和响应式布局，继续使用旧 API。
3. 实施 NPC 观察档案与标签惰性加载。
4. 新增并独立验证聚合快照接口。
5. 渐进迁移首页高频数据，再迁移 NPC 当前标签，保留逐模块旧 API 回退。
6. 完成功能、视觉、响应式、可访问性、请求预算和 30 分钟稳定性验收。

每一步均可单独回退；在第 4 步通过前，不让新聚合接口承担生产仪表盘读取。
