# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是本地运行 MiniWorld 的开发者和运营者。他们同时是模拟世界的观察者与控制者，需要持续观察 NPC 演化、发现运行或行为异常、理解决策原因，并安全控制 Agent 自治。

普通观赏型用户是次要用户；当前产品优先满足高信息密度的观察、诊断和控制任务。

## Product Purpose

MiniWorld 是一个持续运行、可解释、可观察的自主 NPC 世界。它让用户在一个长期演化的五人模拟社会中观察行为、关系、经济、生活事件和认知变化，并在不放弃安全边界与可审计性的前提下选择性启用在线 Agent 自治。

产品成功意味着：用户能迅速判断世界是否安全运行、理解五位 NPC 当前状态和关键变化、定位异常及其原因，并确信模型文本不会绕过 Simulation Engine 直接改写世界事实。

## Positioning

MiniWorld 的差异化机制是“Engine 独占事实、Agent 受限建议、全过程可回退与可审计”。Simulation Engine 始终决定客观事实与动作执行；在线模型只能在明确授权、身份隔离、预算、限流、熔断和二次校验均通过时参与受限决策、会话或反思。失败、停用或无密钥时，世界继续通过 Utility AI 或确定性人格安全运行。

## Operating Context

- 产品在本地以 FastAPI 服务和原生 HTML/CSS/JavaScript 仪表盘运行。
- 用户从仪表盘观察世界时钟、地点、NPC 状态、事件、社会经济、关系、人生故事、Agent 接管、会话、认知及运行审计。
- 用户可以暂停或恢复世界、调整模拟速度、重置当前存档，并显式开启、暂停、继续或紧急停止在线自治。
- 用户需要从世界总览进入单个 NPC，检查其状态、目标、经济、关系、记忆、认知、Agent 建议、最终行动及 Utility 决策依据。
- 首页信息优先级固定为：运行安全与异常 → 世界总览 → 五位 NPC 当前状态 → 关键世界变化与世界脉搏 → 社会经济和故事趋势 → 深度分析与审计。

## Capabilities and Constraints

- 保留 V0.1–V1.6 的全部现有功能、数据含义、控制能力和公开 API 兼容性。
- 首页无需平铺全部模块；低频内容可以通过页内导航、折叠区域或详情视图按需呈现，但不得删除现有信息。
- Simulation Engine 是客观事实与动作执行的唯一权威。模型文本不能直接修改关系、金钱、物品、地点、承诺或行动。
- 在线自治默认关闭，且必须继续受显式授权、逐 NPC 隔离、预算、限流、并发、熔断、超时、取消和安全 fallback 保护。
- 允许增量新增向后兼容的聚合快照接口，但必须保留全部旧接口和字段语义。
- 聚合快照必须返回统一的快照时间或版本；单个子模块失败必须表现为局部错误，不能让整个仪表盘不可用。
- 前端在迁移聚合接口前先建立请求调度、并发锁、AbortController、页面不可见暂停、分频刷新和差异化渲染。
- 聚合接口通过稳定性验证后才能渐进迁移；迁移期间必须保留旧接口回退路径。
- 真实 API Key 只存在于本机进程环境或内存中，不进入仪表盘数据、SQLite、日志或代码。

## Brand Commitments

- 产品名称为 MiniWorld。
- 界面语言以简体中文为主，技术名词与既有版本标识在有助于审计和兼容理解时保留。
- 产品语气应清晰、克制、可解释，明确区分 Engine 事实、模型建议、fallback、估算值与 Provider 状态。
- 现有绿色、米色、石墨色为主的自然实验室视觉身份属于需要记录并精炼的既有资产，不在本次优化中无故替换。

## Evidence on Hand

- `README.md`：V0.1–V1.6 产品能力、安全边界、运行方式和 API 说明。
- `static/index.html`、`static/style.css`、`static/app.js`：现有仪表盘的信息结构、视觉系统、交互和请求模式。
- `api/`、`simulation/`、`database/`：公开接口、Simulation Engine 权限边界和持久化事实模型。
- `tests/` 与 `logs/`：版本回归、稳定性、HTTP 验证和性能验证证据。
- 当前没有用户研究、可用性测试、观赏型用户行为数据或外部品牌资产；后续方案不得虚构这些证据。

## Product Principles

1. **安全状态先于丰富信息。** 用户应先判断系统是否安全、是否异常、是否正在调用 Provider，再进入解释与分析。
2. **五位 NPC 始终可被快速扫读。** 当前地点、行为、核心状态、Agent 模式和异常不得被低频模块淹没。
3. **解释必须贴近事实来源。** Engine 事实、Utility 评分、Agent 建议、最终行动和 fallback 原因需要清晰分层。
4. **渐进披露而不丢失信息。** 高频信息默认可见，低频分析按需展开，但全部现有数据仍可访问。
5. **性能优化必须保持可回退。** 新快照与前端调度逐步接管读取流量，旧 API 和安全控制路径持续可用。

## Accessibility & Inclusion

仪表盘应支持键盘导航、清晰焦点、足够的文本与状态对比度、非颜色唯一的状态表达、可预测的对话框焦点管理，以及在窄屏和触控设备上可用的控制尺寸。动态刷新不得无故打断焦点、阅读位置或辅助技术的当前上下文。
