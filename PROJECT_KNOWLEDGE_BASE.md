# MiniWorld 项目实施知识库

> 适用范围：MiniWorld V0.1–V1.6 的建设复盘，以及后续同类项目的立项、开发、验收、交付和维护。
>
> 使用方式：新项目启动时从第 4 章开始照表执行；开发具体版本时使用第 5–7 章；交付和复盘使用第 8–10 章。本文记录方法和入口，实时配置、API 与验证结果仍以 `README.md` 为准。

## 1. 先说结论：这个项目是怎么做成的

MiniWorld 的实现策略不是先接大模型，也不是一次做完所有玩法，而是按“确定性事实内核 → 可解释玩法闭环 → 产品化 → 受限 Agent → 在线运行安全”的顺序逐层增加能力。

核心原则只有一句话：

> Simulation Engine 决定并写入客观世界事实；Utility AI 或 Agent 只能在 Engine 提供的候选范围内做选择；LLM 文本不能直接修改金钱、关系、位置、状态、职业、物品、住房等事实。

真实建设顺序如下：

1. 先建立持续运行的世界、时钟、NPC、地点、动作和 Utility AI，让系统在完全离线、没有模型时也能运行。
2. 再给 NPC 增加记忆、长期目标和关系，使短期动作可以积累为长期状态。
3. 将 LLM 放到叙事层，只生成对话、解释、总结等文字，并提供确定性回退。
4. 依次加入经济、职业预算、社区节奏、群体关系和人生故事，每次只增加一个可验证闭环。
5. 补齐多存档、导入导出、统计、性能、稳定性和新手引导，把“技术演示”变成可交付产品。
6. 先让 Agent 做影子建议，不执行；验证稳定后才允许接管一个 NPC，再扩展到五个 NPC。
7. Agent 接管仍需 Engine 提供候选、校验参数、在执行前二次校验，失败时回退到 Utility AI。
8. 在动作接管稳定后，才增加多轮社交、每日反思和非执行计划。
9. 最后统一在线运行时，补齐显式授权、隔离、公平并发、限流、预算、熔断、审计、取消、急停和故障恢复。
10. 每个版本都通过旧功能回归、数据库升级、真实 HTTP、长周期模拟、完整性检查和文档更新后才进入下一版。

这个顺序的价值是：每一层都能独立运行、独立验证、出现问题时独立降级；模型不可用只会降低智能表现，不会摧毁世界或阻塞模拟。

## 2. 证据边界

本知识库基于以下仓库证据整理：

- `README.md` 中 V0.2–V1.6 Roadmap、核心系统、API、交付验证和统一交付门槛。
- `simulation/` 中按领域拆分的实现，以及 `WorldService` 的统一 Tick 编排。
- `database/database.py` 中 V0.9–V1.6 分层、隔离、增量建表和 V1.3 兼容迁移。
- `tests/` 中从基础动作到 V1.6 运行时的单元、API、迁移、故障与长期稳定性测试。
- `scripts/` 中性能、真实 HTTP、正式库升级、30/90/365 日和 V1.6 30 日验证脚本。
- `PRODUCT.md`、`DESIGN.md`、`DASHBOARD_REDESIGN_PLAN.md` 中的产品、设计和专项改造约束。

当前仓库 Git 历史只有一个 `Initial commit`。因此，第 3 章是根据版本化文档、模块、表和测试还原的产品演进顺序，不是逐个 Git commit 的时间线。后续项目应从第一天开始保留小步提交和变更记录，避免只能从最终状态反推过程。

## 3. MiniWorld 的真实实现路径

### 3.1 阶段 A：先做可运行的模拟内核

基础版本先解决四件事：

- 世界时间能够按 Tick 持续推进、暂停和变速。
- 五个 NPC 有状态、性格、职业、位置和关系。
- 动作有前置条件、持续时间和确定性结果。
- Utility AI 根据需求、位置、时间和关系对候选动作评分。

这一步形成最小闭环：

```text
读取当前世界事实
        ↓
Engine 生成合法候选
        ↓
Utility AI 评分并选择
        ↓
Engine 开始/完成动作
        ↓
写入状态、事件和决策日志
        ↓
时钟进入下一个 Tick
```

关键实现位于：

- `simulation/clock.py`：世界时间和 Tick 粒度。
- `simulation/decision.py`：候选动作与 Utility 评分。
- `simulation/actions.py`：动作开始、完成和状态变化。
- `simulation/world.py`：初始化、Tick 编排、事务提交与快照。
- `database/models.py`：持久化事实模型。
- `api/world.py`、`api/npc.py`：观察与控制入口。

这一步的验收不是“页面能打开”，而是固定 seed 下可以重复运行，动作合法，状态范围不越界，暂停/继续/重置可用，数据库事实一致。

### 3.2 阶段 B：从短期动作扩展到长期人物

V0.2–V0.3 增加记忆和目标：

- 记忆带重要度、情绪、时间和关联 NPC。
- 长期目标覆盖储蓄、社交、职业满意度和关系建设。
- 目标不是展示文本，而是进入 Utility 决策上下文，影响后续行动。

这一步让“刚刚发生的动作”可以影响“以后想做什么”。实现时仍保持 Engine 事实为唯一来源，避免记忆或目标文本反向篡改世界。

### 3.3 阶段 C：先限制 LLM 权限，再使用 LLM

V0.4 才加入 LLM，而且只负责：

- 对话文本。
- 重要事件解释。
- 长期目标文字表达。
- 记忆总结。

实现边界是：

```text
Engine 已提交事实 ──→ 结构化叙事任务 ──→ LLM/模板 ──→ 只读文字产物
        ↑                                              │
        └──────────── 不允许反向写世界事实 ────────────┘
```

缺 Key、超时、HTTP 错误、非法 JSON、错误说话人或空文本都会走确定性中文模板。模拟循环不会因为叙事服务失败而回滚或停止。

### 3.4 阶段 D：一次增加一个玩法闭环

V0.5–V0.9 没有同时堆功能，而是每版增加一个可以独立解释和验证的闭环。

| 版本 | 新闭环 | 输入 | Engine 事实产出 | 主要验证 |
| --- | --- | --- | --- | --- |
| V0.5 | 社会经济 | 工作、商店、物品、住房 | 工资、购买、库存、技能、租金、流水 | 金额守恒、原子扣减、不可重复消费 |
| V0.6 | 职业与预算 | 表现、技能、支出、就业状态 | 评估、加薪、晋升、失业/求职、周报、压力 | 风险上限、原因可解释、旧路径兼容 |
| V0.7 | 社区节奏 | 排班、营业时间、库存、设施名额 | 出勤、迟到、培训、补货、住房升级 | 时间窗、名额、周期处理、周末差异 |
| V0.8 | 群体社会 | 双向关系、互动、承诺 | 关系阶段、信任、朋友圈、共同活动、合住 | 有向/无向一致、衰减修复、人数上限 |
| V0.9 | 人生故事 | 已提交的职业、经济、关系等事实 | 里程碑、因果链、周/月总结、回放点 | 去重、来源可追溯、固定 seed 回放 |

每个闭环采用相同模式：

1. 先定义范围、非目标、事实所有权和失败回退。
2. 增量增加数据表，不覆盖旧事实。
3. 增加初始化和周期处理，但用 savepoint 隔离新功能故障。
4. 把新上下文增量接入 Utility 决策；资料缺失时只移除该层能力。
5. 增加 API 和 Dashboard 观察入口。
6. 补单元、API、禁用、故障、重置、迁移和多日测试。
7. 全量回归旧版本。

### 3.5 阶段 E：把演示系统产品化

V1.0 处理的是“能否长期、放心地使用”，而不是继续堆玩法：

- 多存档和预设。
- 新世界配置。
- SQLite writer ownership，防止两条模拟循环同时写一个存档。
- 安全导入导出和校验包。
- 统计、平衡守护和升级报告。
- 新手引导。
- 批量 Tick 性能优化。
- 30/90/365 日全 Tick 稳定性验证。

这一阶段标志着开发目标从“功能存在”切换为“数据可保、故障可恢复、性能可量化、用户会使用”。

### 3.6 阶段 F：Agent 从旁观到有限接管

Agent 能力按风险逐级开放：

| 版本 | Agent 权限 | 为什么这样排 |
| --- | --- | --- |
| V1.1 | Alice 影子建议，只比较不执行 | 先验证感知、提示结构、异步队列和建议质量 |
| V1.2 | Alice 显式接管 | 单人控制爆炸半径，验证租约、超时和执行前二次校验 |
| V1.3 | 五个独立 Agent | 单人链路稳定后才解决隔离、公平并发和逐人恢复 |
| V1.4 | 已确认社交动作上的多轮会话 | 先有合法动作，再允许在动作内部生成多轮文字 |
| V1.5 | 每日反思、信念、非执行计划 | 只形成主观认知；计划进度必须由真实事件推动 |
| V1.6 | 统一在线运行时 | 最后统一处理授权、预算、限流、熔断、审计、取消和急停 |

Agent 接管的安全链路是：

```text
Engine 生成候选与有效期
        ↓
持久任务进入有界队列
        ↓
Agent 返回严格 JSON 建议
        ↓
校验结构、NPC 归属、动作和参数
        ↓
按最新世界状态再次生成候选并二次校验
        ↓
合法：Engine 执行 / 非法、超时、过期、故障：Utility fallback
        ↓
审计闭环，任何路径都不能停住世界
```

V1.6 再为全部在线调用增加统一门禁：

- 用户必须显式启动 online；有 Key 不等于授权调用。
- 任务按 `npc_id/task_type` 隔离。
- 全局、逐 NPC、逐任务调用与 Token 预算。
- 总并发、逐任务并发和有界队列。
- 超时、有限重试、熔断和恢复。
- generation 防止停止后的迟到响应写入。
- 普通停止和紧急停止。
- metadata-only 调用审计，不保存 prompt、response、密钥或隐藏思维链。

### 3.7 阶段 G：仪表盘按“安全优先”重构

仪表盘没有只做视觉美化，而是先写 `DASHBOARD_REDESIGN_PLAN.md`，用源码证据确认问题，再定义：

- 首屏顺序：安全与运行状态 → 世界总览 → 五位 NPC → 世界脉搏。
- NPC 档案五个标签和惰性加载。
- 聚合快照与旧 API 回退。
- 同键并发锁、分频轮询、页面隐藏暂停和局部错误。
- 响应式、键盘、焦点、触控尺寸和 reduced-motion。
- 请求数、首屏时间和渲染耗时预算。
- 文件改动范围、实施顺序和逐项验收标准。

这体现了正确的专项改造方式：先测现状，先写目标和预算，再动实现。

## 4. 可复用的项目全生命周期 SOP

### 4.1 项目开始前：先把决定写下来

立项阶段至少完成以下七件事。

#### 1. 写一页产品定义

回答：

- 为谁解决什么问题？
- 用户最重要的一个动作是什么？
- 成功是什么，失败是什么？
- 当前版本必须做什么？
- 明确不做什么？
- 有哪些不可突破的业务、安全和合规边界？

产物：`PRODUCT.md` 或 `docs/PRODUCT_BRIEF.md`。

#### 2. 写范围与版本路线

不要只列功能名，要把每个版本写成一个可闭环、可验收的增量：

```text
版本目标：解决哪个完整问题
新增能力：用户能多做什么
不包含：本版刻意不做什么
兼容要求：哪些旧行为绝不能变
失败回退：新功能不可用时系统怎么继续
验收证据：用什么测试、指标和产物证明完成
```

产物：`ROADMAP.md`；MiniWorld 当前将它放在 `README.md`。

#### 3. 写系统边界和事实所有权

列出每类数据由谁读取、谁能写、谁只能建议。涉及 AI 时必须额外写清：

- 模型可见什么。
- 模型可输出什么。
- 输出如何校验。
- 哪些字段模型永远不能改。
- 超时、无 Key、限额、越权时如何回退。

产物：`docs/ARCHITECTURE.md` 和必要的 ADR。

#### 4. 先设计数据和升级策略

明确：

- 核心实体、主键、唯一约束、外键和状态机。
- 事实表、派生表、审计表、文本产物的区别。
- 新表还是改旧表。
- 如何升级旧库、如何证明旧事实没变。
- 迁移失败、重复运行、部分成功时的行为。

产物：`docs/DATA_MODEL.md`、`docs/MIGRATION_PLAN.md`。

#### 5. 先写 API 契约

至少写方法、路径、输入、输出、错误、幂等性、权限和兼容要求。API 不是实现完再补的目录，而是前后端和测试共同使用的合同。

产物：`docs/API_CONTRACT.md`；MiniWorld 目前由 FastAPI OpenAPI 与 README API 表共同承担。

#### 6. 先写验收计划

把“完成”翻译成可执行检查：

- 功能正常路径。
- 边界和非法输入。
- 故障与降级。
- 旧版本兼容。
- 数据库升级与回滚。
- 性能和长期稳定性。
- 安全、隐私、可访问性。
- 真实服务烟测。

产物：`docs/TEST_PLAN.md` 或版本计划中的验收章节。

#### 7. 建立变更纪律

从第一天执行：

- 一个提交只做一个逻辑变更。
- 提交信息说明“为什么”，不是只写“update”。
- 每个版本保留变更记录和交付证据。
- 重要架构决定使用 ADR，避免未来重复争论。
- 秘密不进入代码、聊天、数据库、日志、测试证据和示例配置。

### 4.2 开发中：按垂直切片闭环

每个功能切片按以下顺序执行：

1. **确认需求与非目标**：先更新版本计划，不在编码中偷偷扩大范围。
2. **定义事实和状态机**：列状态、转换、所有权、幂等性和失败终态。
3. **写测试场景**：至少先列正常、边界、故障、禁用和兼容案例。
4. **做增量数据变更**：优先加表/加字段；迁移必须可重复、可校验。
5. **实现领域逻辑**：事实变更集中在领域服务/Engine，不散落在 API 或页面。
6. **接入事务边界**：一次 Tick 或业务动作要么完整提交，要么安全回退；新层故障不污染旧层。
7. **增加 API**：输入严格验证，错误可理解，不改旧响应形状。
8. **增加观察能力**：日志、事件、审计、健康状态和 Dashboard 同步补齐。
9. **执行分层测试**：先相关单元，再模块/API，再全量回归。
10. **更新文档**：配置、API、限制、验证结果和运维步骤与代码同批提交。

推荐的变更闭环：

```text
需求/边界
   ↓
数据与契约
   ↓
领域实现
   ↓
API/界面/可观察性
   ↓
专项测试
   ↓
兼容与故障测试
   ↓
真实服务与长期验证
   ↓
文档和交付证据
```

### 4.3 每天开发结束前

当天至少做到：

- 代码能启动，相关测试通过。
- 工作区没有误提交的数据库、日志、密钥或临时文件。
- 新配置写入示例文件，默认值安全。
- 未完成项显式记录，不把半成品伪装为完成。
- 新事实能通过 API、日志或审计观察。
- 有破坏风险的变更有备份、回退或隔离验证方案。

### 4.4 一个版本做完后

版本交付不是“代码写完”，而是以下门槛全部通过：

1. 功能验收标准逐项通过。
2. 新增专项测试通过。
3. 全量旧测试通过。
4. 旧库升级在备份和隔离环境中通过。
5. SQLite `integrity_check` 通过，`foreign_key_check` 为 0。
6. 固定 seed 或等价机制证明关键流程可复现。
7. 真实 Uvicorn 启动，Dashboard、Docs、OpenAPI 和关键 API 通过。
8. 长周期运行没有队列泄漏、未终结任务、范围越界或持续性能退化。
9. 故障注入覆盖超时、非法输出、限额、暂停/停止/重启等场景。
10. README、配置示例、API 表、运行手册和验证结果已更新。
11. 正式数据、临时验证数据和真实外部调用严格隔离。
12. 保存结构化验证报告和一份人能读懂的交付检查点。

只有门槛全部通过，版本状态才能从“开发中”改为“完成”。

### 4.5 上线或交付后

交付后继续做：

- 观察错误率、延迟、队列、预算、熔断、数据完整性和资源占用。
- 定期验证备份可恢复，而不是只确认“有备份文件”。
- 将线上问题写成复盘：现象、影响、时间线、根因、修复、预防项。
- 把重复问题转化为测试、告警、运行手册或自动化脚本。
- 定期清理已失效的配置、截图和验证数字；保留历史证据但标明版本。
- 下一版先更新路线和边界，再进入开发。

## 5. 文档体系：要写哪些、写什么、怎么用

### 5.1 必备文档矩阵

| 文档 | 回答的问题 | 必写内容 | 谁在什么时候用 | 更新时机 |
| --- | --- | --- | --- | --- |
| `README.md` | 如何理解、安装、启动和验证当前版本 | 项目简介、快速开始、配置、结构、核心能力、API 入口、测试、当前限制 | 所有人第一次进入项目；交付验收 | 用户用法、配置、接口或验证状态变化时 |
| `PRODUCT.md` | 为什么做、给谁做、产品坚持什么 | 用户、目的、定位、场景、能力与约束、产品原则、可访问性 | 产品、设计、研发在范围争议时 | 用户、目标或产品边界变化时 |
| `ROADMAP.md` | 先做什么、后做什么 | 版本目标、依赖、非目标、验收门槛 | 立项、排期、版本切换 | 每个版本开始和完成时 |
| `ARCHITECTURE.md` | 系统如何协作、边界在哪里 | 组件图、调用流、事务、事实所有权、故障隔离、部署形态 | 开发新模块、排查跨层问题 | 组件职责或关键流程变化时 |
| `DATA_MODEL.md` | 数据代表什么、如何保持一致 | 实体、约束、状态机、事实/派生/审计分类、保留策略 | 后端、测试、数据排障 | 表、字段、状态或约束变化时 |
| `API_CONTRACT.md` | 模块之间如何通信 | 路径、输入、输出、错误、幂等、兼容、示例 | 前后端联调、自动测试、外部集成 | API 变化前先更新 |
| `VERSION_PLAN.md` | 当前这一次具体怎么改 | 背景证据、目标、范围、文件、步骤、风险、回退、验收 | 当前版本的所有执行者 | 开发前建立，过程中持续维护 |
| `TEST_PLAN.md` | 如何证明做对且没破坏旧功能 | 测试矩阵、环境、数据、命令、预期、性能预算、失败处理 | 开发、自测、QA、交付 | 新风险或验收标准出现时 |
| `RUNBOOK.md` | 出问题或要操作时怎么做 | 启停、备份恢复、迁移、监控、告警、急停、常见故障 | 运维、值班、交付接手人 | 每次操作经验或事故后 |
| `CHANGELOG.md` | 每个版本改变了什么 | 新增、修改、修复、兼容、安全、升级注意 | 用户、交付、维护者 | 每版发布时 |
| `docs/adr/*.md` | 为什么选择这个方案 | 背景、候选方案、决定、影响、回退条件 | 未来修改架构或质疑旧决定时 | 发生不可轻易逆转的技术决定时 |
| `RELEASE_CHECKPOINT.md` | 这版凭什么算完成 | 命令、结果、环境、数据隔离、已知问题、停止状态 | 发布审批、交接、复盘 | 每次候选发布和最终发布 |

小项目不需要为了文档数量而拆文件。可以把 Roadmap、API、Changelog 暂时放在 README，但上述问题必须有明确、唯一、可维护的答案。

### 5.2 文档内容模板

#### 产品定义模板

```markdown
# 产品定义

## 用户与场景
谁在什么情况下使用？

## 问题
当前痛点和证据是什么？

## 目标
本阶段要改变什么结果？

## 成功指标
用什么量化或验收？

## 范围
必须做什么？

## 非目标
明确不做什么？

## 原则与边界
安全、隐私、兼容、事实所有权、成本边界是什么？
```

#### 版本实施计划模板

```markdown
# <版本/功能> 实施计划

## 背景与源码证据
现状、问题、数据和对应文件位置。

## 目标与非目标
做什么，不做什么。

## 用户流程
变更前后用户如何操作。

## 技术方案
组件、数据、API、状态机、事务和失败回退。

## 兼容与迁移
旧 API、旧数据、旧配置如何保持。

## 安全与隐私
秘密、权限、审计、外部调用和急停。

## 文件改动清单
每个文件为什么改。

## 实施顺序
每一步的输入、输出、确认点和可回退点。

## 测试矩阵
正常、边界、禁用、故障、升级、长期、真实服务。

## 验收标准
可勾选、可执行、可提供证据。

## 风险与回退
风险、触发条件、隔离方法和恢复步骤。
```

#### ADR 模板

```markdown
# ADR-XXX：<决定标题>

- 状态：提议 / 已接受 / 已废弃 / 被替代
- 日期：YYYY-MM-DD

## 背景
为什么必须决定？

## 候选方案
各方案的收益、成本和风险。

## 决定
选择什么，为什么。

## 影响
带来哪些约束、迁移和维护成本。

## 重新评估条件
出现什么情况时重新选择。
```

#### 交付检查点模板

```markdown
# <版本> 交付检查点

## 交付范围
本次完成和未完成的内容。

## 环境
系统、运行时、依赖、数据库副本、外部服务状态。

## 验证结果
命令、通过数、耗时、报告路径。

## 数据验证
迁移前后表、行数、关键事实、integrity_check、foreign_key_check。

## 真实服务验证
启动方式、端口、关键页面/API、停止方式。

## 安全与费用
是否使用真实 Key、调用数、Token/费用、秘密扫描结果。

## 已知问题与边界
未解决问题、影响和临时措施。

## 最终状态
进程是否停止、世界是否暂停、队列是否为 0、下一步是否获批。
```

### 5.3 文档不是写完存档，而是工作工具

正确用法：

- **评审需求时**：用 `PRODUCT.md` 和 Roadmap 判断是否属于本版。
- **设计方案时**：用架构、数据模型和 ADR 约束实现方向。
- **前后端联调时**：以 API 契约为共同基准，不靠口头记忆。
- **开发过程中**：把版本计划当检查表，完成一项勾一项。
- **写测试时**：从验收标准反推测试矩阵，避免只测正常路径。
- **排障时**：先查 Runbook、状态机和事实所有权，再定位代码。
- **交付时**：Release Checkpoint 提供可复现证据，不只说“我测过了”。
- **新人上手时**：按 README → Product → Architecture → 当前版本计划 → Runbook 阅读。
- **做下一版时**：先读上版已知问题和 ADR，避免破坏旧边界。

错误用法：

- 代码已经改完才补一份与实现不一致的方案。
- 在多个文档复制同一份实时配置，最后互相冲突。
- 写“支持高并发”“性能很好”，却没有预算、测试方法和数字。
- 只记录成功结果，不记录环境、失败修复和最终停止状态。
- 把密钥、真实 prompt/response、个人信息或生产数据放进示例和证据。

## 6. MiniWorld 当前文档如何分工

| 现有文件 | 当前职责 | 使用建议 |
| --- | --- | --- |
| `README.md` | 当前版本权威入口：启动、安全配置、玩法、API、测试、交付证据、Roadmap | 配置或行为变化必须同步更新；历史数字标明版本 |
| `PRODUCT.md` | 产品用户、目的、定位、约束、品牌承诺和可访问性原则 | 功能取舍时先核对，不把系统变成开放式不可控 Agent |
| `DESIGN.md` | 仪表盘视觉系统、组件、布局和设计禁忌 | 改 UI 前先读，新增组件复用令牌和交互原则 |
| `DASHBOARD_REDESIGN_PLAN.md` | 仪表盘专项改造的证据、边界、性能预算、文件范围和验收 | 是高质量 `VERSION_PLAN.md` 的现成范例 |
| `.env.example` | 无秘密的在线运行配置合同 | 新增环境变量时写用途、安全默认值和合理范围，不填真实值 |
| `tests/` | 可执行的行为和兼容知识 | 改模块前先读同名测试；修复缺陷时先增加回归用例 |
| `scripts/` | 可重复的交付验证知识 | 不用手工步骤替代已有脚本；报告写入隔离路径 |

当前还可逐步补齐：

- `docs/ARCHITECTURE.md`：把散落在 README 和代码中的组件、事务、状态机集中起来。
- `docs/DATA_MODEL.md`：数据库表较多，建议按版本和领域给出表关系与事实分类。
- `docs/RUNBOOK.md`：集中启停、备份恢复、迁移、急停和常见故障。
- `CHANGELOG.md`：从后续版本开始保留面向用户的增量变更。
- `docs/adr/`：记录单 worker、Engine 事实所有权、增量迁移和 metadata-only 审计等关键决定。

## 7. 测试和验证怎么分层使用

### 7.1 开发反馈层

每次小改先跑：

1. 语法/静态检查。
2. 与改动模块同名的单元测试。
3. 对应 API 或前端测试。

目的：几分钟内发现局部错误。

### 7.2 版本回归层

功能稳定后跑：

- 全量 `pytest`。
- 前端行为测试。
- 禁用开关、无 Key、超时、非法结构和兼容路径。
- 重置、重复初始化和升级幂等。

目的：证明新增层没有破坏旧层。

### 7.3 交付验证层

候选版本再跑：

- 旧库保护性升级。
- SQLite 完整性和外键检查。
- 真实本地 Uvicorn + HTTP/OpenAPI/Dashboard 验收。
- 性能基准。
- 30/90/365 日或版本要求的长期模拟。
- 队列、租约、审计、暂停、重启、急停和故障注入。
- 如确有需要且得到明确授权，再做最小真实 Provider 烟测。

目的：证明系统在真实进程、真实持久化和长时间条件下成立。

### 7.4 测试数据纪律

- 单元测试使用临时数据库。
- HTTP 验收使用独立验证数据库并避免第二条后台模拟循环。
- 正式库升级前先做可恢复备份，比较升级前后旧表和关键事实。
- 自动验收使用 fake/mock Provider，默认不访问网络、不读真实 Key、不产生费用。
- 验证报告只保存必要元数据，不保存秘密和敏感原文。
- 只停止经过端口、PID、启动时间核验的本次验证进程。

## 8. 安全和兼容清单

涉及数据库、Agent 或长期运行时，每版至少检查：

- [ ] 新功能默认值是否安全。
- [ ] 无配置、无 Key、禁用和外部故障时旧功能是否继续。
- [ ] 是否仍只有领域 Engine 能写客观事实。
- [ ] Agent 输出是否结构校验、归属校验和执行前二次校验。
- [ ] 是否有任务有效期、租约、取消和最终状态。
- [ ] 队列和并发是否有上界，是否保证不同 NPC/任务公平。
- [ ] 停止后的迟到响应是否会被拒绝。
- [ ] 是否有预算、限流、熔断、急停和审计。
- [ ] 日志、数据库、前端存储和验证报告是否不含秘密。
- [ ] 数据迁移是否增量、幂等、可校验、可恢复。
- [ ] 旧 API 字段、旧随机调用顺序和旧持久化语义是否保持。
- [ ] 单个新模块失败是否被 savepoint/事务边界隔离。
- [ ] 重启后是否能恢复或安全终结未完成任务。

## 9. Definition of Done：统一完成定义

一个功能只有同时满足以下条件才算完成：

- 用户结果已实现，非目标没有被偷偷带入。
- 领域规则、数据约束、API 和 UI 一致。
- 正常、边界、故障、禁用和兼容测试已覆盖。
- 新旧数据升级与完整性已验证。
- 性能和长期运行满足明确预算。
- 日志、审计、健康检查和故障处置可用。
- 安全、隐私、成本和权限边界已检查。
- README、配置、契约、运行手册和交付证据已更新。
- 没有遗留活动任务、未知进程、临时密钥或未说明风险。
- 评审者可以按文档在新环境复现关键结果。

## 10. 新成员或新任务的推荐阅读路径

### 新成员 60 分钟上手

1. `README.md`：理解产品、启动方式和当前版本。
2. `PRODUCT.md`：理解用户、目标和不可突破的边界。
3. 本知识库第 1–3 章：理解项目为什么按这个顺序演进。
4. `simulation/world.py`：查看统一 Tick 和事务入口。
5. `simulation/decision.py`、`simulation/actions.py`：理解候选、选择和执行。
6. `database/database.py`：理解增量 Schema 与兼容策略。
7. 与任务同名的 `simulation/*.py` 和 `tests/test_*.py`。
8. `DASHBOARD_REDESIGN_PLAN.md`：学习如何把专项工作写成可执行方案。

### 开始一个新功能

1. 在 Product/Roadmap 中确认属于当前范围。
2. 复制第 5.2 节版本实施计划模板。
3. 列事实所有权、数据变更、API 契约和失败回退。
4. 列测试矩阵和交付证据。
5. 评审方案后按第 4.2 节闭环开发。
6. 使用第 8、9 章完成安全检查和 DoD。

### 修复一个缺陷

1. 记录可复现步骤和影响范围。
2. 确认错误发生在事实层、编排层、API 层还是展示层。
3. 先增加能稳定复现的回归测试。
4. 做最小修复，不借机扩大需求。
5. 跑相关测试和受影响的旧版本测试。
6. 若属于常见操作问题，更新 Runbook；若属于设计决定问题，补 ADR。

## 11. 这套方法最值得复用的经验

1. **先有离线确定性内核，再加 AI。** AI 应是可降级能力，不是系统活着的前提。
2. **先影子、后单点接管、再扩大范围。** 每一级只增加一种风险。
3. **事实、建议和文字分层。** 谁能写什么必须落到代码、数据模型和测试中。
4. **每版只增加一个完整闭环。** 小闭环更容易迁移、回归、降级和解释。
5. **兼容性是功能。** 旧 API、旧库、旧随机路径和旧玩法必须自动验证。
6. **故障回退与正常功能同时设计。** 不能等外部服务出错后才补。
7. **长期运行必须用长期测试证明。** 单次接口成功不能证明队列、租约和周期逻辑正确。
8. **文档应产生行动。** 每篇文档都要对应评审、开发、验收、操作或复盘场景。
9. **交付要留证据。** 命令、环境、结果、数据检查、已知问题和最终停止状态缺一不可。
10. **完成后真的停下来。** 未通过交付门槛不进入下一版；没有新授权不扩大外部调用或项目范围。

